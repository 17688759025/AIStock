# 竞价采集恢复

## 两阶段保存

1. `node scripts/collect_auction_result.cjs --capture-only`：提前等待09:25:03；新浪失败切东财，整轮失败后退避3/6/9/12/15秒，在09:29:40前继续尝试。请求剩余时间有硬截止，返回跨过09:30的数据仍拒绝。
2. 合格报价立即保存为 `data/auction/YYYY-MM-DD.capture.json`。这是标准化、未评分的全市场报价，保留采集起止时间、来源及内容校验和；非原始HTTP响应。既有完整性与行情时点校验不放宽。
3. Actions先提交capture文件，再执行 `--score-only`，最后提交result文件。补板块分类、昨日历史量价及资金允许在09:30后进行；报价和成交额只取capture文件，历史日线与资金只用交易日前数据。评分网络预算独立为10分钟。
4. 后续任务发现当日capture但缺result，可以复用当日capture完成评分。没有有效capture则09:30后仍拒绝从盘中行情回填。已有result不覆盖。

## 失败保全

- 采集轮次、来源、失败原因、保存阶段记录到`/tmp/auction-collector-diagnostics.jsonl`。
- 无论成功失败，Actions尝试上传capture、result及诊断artifact，保留14天。任务被平台强制终止等情况不保证artifact成功。
- 发布最多尝试3次，禁止force push；rebase冲突明确失败，不自动覆盖其他数据。
- 评分失败时，已经提交的capture仍在仓库；后续定时任务或手动运行工作流可恢复。上传artifact不等于已发布到仓库。

## 边界

GitHub定时任务仍可能完全错过窗口；所有行情源在窗口内持续不可用时仍会缺快照。本改动减少“一次失败就退出”和“报价成功却因评分迟到而丢弃”，不保证每天一定成功。网页本机重建仍不能冒充正式历史。

离线测试覆盖退避、截止、在途请求排空、日期/校验和校验、评分失败后的报价保留、延迟评分及工作流步骤顺序。未以盘中数据伪造正式快照来测试。
